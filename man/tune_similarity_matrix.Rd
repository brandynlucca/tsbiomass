% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{tune_similarity_matrix}
\alias{tune_similarity_matrix}
\title{Empirically tune the similarity configuration}
\usage{
tune_similarity_matrix(
  candidate_models,
  species_traits = NULL,
  study_traits = NULL,
  alpha = NULL,
  k_species = NULL,
  k_study = NULL,
  max_models_per_species = NULL,
  n_resamples = NULL,
  seed = NULL,
  config = NULL,
  cache_path = NULL,
  refresh = NULL,
  progress = NULL,
  registry_path = NULL
)
}
\arguments{
\item{candidate_models}{Prepared candidate-model table.}

\item{species_traits}{Optional species-trait specification. See
\code{\link[=prepare_similarity_matrix]{prepare_similarity_matrix()}} for the accepted forms. When \code{NULL}, a
config-supplied value is used when present.}

\item{study_traits}{Optional study-trait specification. See
\code{\link[=prepare_similarity_matrix]{prepare_similarity_matrix()}} for the accepted forms. When \code{NULL}, a
config-supplied value is used when present.}

\item{alpha}{Optional starting species-versus-study mixing parameter. When
\code{NULL}, a config-supplied value is used when present.}

\item{k_species}{Optional starting species-distance kernel parameter. When
\code{NULL}, a config-supplied value is used when present.}

\item{k_study}{Optional starting study-distance kernel parameter. When
\code{NULL}, a config-supplied value is used when present.}

\item{max_models_per_species}{Maximum number of retained tuning models per
species. When \code{NULL}, a config-supplied value is used when present.}

\item{seed}{Optional integer seed. When \code{NULL}, a config-supplied value is
used when present; otherwise one is generated and returned in the output
object.}

\item{config}{Optional JSON path or list with similarity options. Supported
entries are \code{species_traits}, \code{study_traits}, \code{alpha}, \code{k_species},
\code{k_study}, \code{max_models_per_species}, \code{seed}, \code{length_coherence},
\code{depth_coherence}, \code{frequency_coherence}, \code{alpha_grid},
\code{k_species_grid}, \code{k_study_grid}, and \code{grid_refinement_levels}. A
\link{WorkflowConfig} object is also accepted.}

\item{cache_path}{Optional path to an \code{.rds} cache file.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache file.}

\item{progress}{Logical scalar controlling stage messages.}

\item{registry_path}{Optional path to a trait-registry JSON file.}
}
\value{
When \code{candidate_models} is a \link{Candidates} object, returns that
object with the tuning result stored in \verb{@similarity_tuning}. Otherwise,
returns a list containing the tuned configuration, tuning subset, score
history, and per-trait component-impact summary.
}
\description{
Builds a reduced tuning subset, prepares the selected similarity inputs,
evaluates the supplied similarity
configuration by leave-one-out error, tunes the alpha and kernel parameters
on a local grid, then drops each selected trait and enabled coherence term
to derive tuned weight multipliers.
}
\examples{
\dontrun{
candidates <- as_candidates(list(
  study = list(path = "input.xlsx"),
  anchors = list(selector = list(regional_body = "SWFSC"))
))

workflow_cfg <- as_configurer(list(
  paths = list(
    input_file = "input.xlsx",
    out_root = "outputs",
    cache_dir = "cache"
  ),
  workflow = list(),
  tuning = list(
    max_models_per_species = 2L,
    n_resamples = 8L,
    grid_refinement_levels = 1L
  ),
  policy = list(
    alpha = 0.8,
    k_species = 4,
    k_study = 2,
    frequency_coherence_mode = "overlap",
    length_overlap_weight = 2,
    depth_overlap_weight = 2,
    frequency_coherence_weight = 1,
    species_traits = list(genus = 1, family = 1),
    study_traits = list(frequency = 1, fao_area = 1)
  ),
  policies = list(active = "all_models_weighted")
))

tune_obj <- tune_similarity_matrix(
  candidate_models = candidates,
  config = workflow_cfg
)
tune_obj@similarity_tuning
}

}
