% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policyselector.R
\name{plot.PolicySelector}
\alias{plot.PolicySelector}
\title{Plot a \code{PolicySelector}}
\usage{
\method{plot}{PolicySelector}(
  x,
  y = NULL,
  type = c(
    "strategy_error_heatmap",
    "conformal_scores",
    "policy_benchmark"
  ),
  anchor_species,
  max_policies = 30L,
  show_values = NULL,
  ...
)
}
\arguments{
\item{x}{A \link{PolicySelector} object.}

\item{y}{Unused.}

\item{type}{Figure family to draw.}

\item{anchor_species}{Optional anchor species to plot. When omitted, the
selector's configured reference-anchor species are used when available.
Use \code{anchor_species = NULL} explicitly to keep all species.}

\item{max_policies}{Maximum number of policies shown in dense benchmark and
uncertainty plots.}

\item{show_values}{Optional logical scalar controlling cell labels in
heatmaps. \code{NULL} lets the plotting helper decide from grid size.}

\item{...}{Additional plotting arguments.}
}
\value{
A ggplot object.
}
\description{
Uses the package's S7 method on \code{\link[base:plot]{base::plot()}} so selector-stage
summaries can be drawn directly from the package object.
}
\examples{
\dontrun{
selector <- benchmark(as_policyselector(candidates))
plot(selector, type = "policy_benchmark")
plot(selector, type = "strategy_error_heatmap")
}
}
