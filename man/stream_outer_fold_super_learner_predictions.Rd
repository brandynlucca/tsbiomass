% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_policy.R
\name{stream_outer_fold_super_learner_predictions}
\alias{stream_outer_fold_super_learner_predictions}
\title{Predict super-learner scores for one outer crossfit fold with streaming fits}
\usage{
stream_outer_fold_super_learner_predictions(
  train_data,
  test_data,
  feature_cols,
  outcome_transform,
  lambda_rule,
  inner_folds,
  seed,
  super_methods = NULL,
  metalearner_loss = c("squared_error", "absolute_error"),
  method_settings = NULL,
  progress = FALSE
)
}
\arguments{
\item{train_data}{Model-ready training split for this fold.}

\item{test_data}{Test split for this fold.}

\item{feature_cols}{Feature columns.}

\item{outcome_transform}{Outcome transform.}

\item{lambda_rule}{Regularization rule.}

\item{inner_folds}{Inner CV folds.}

\item{seed}{Integer seed.}

\item{super_methods}{Optional base methods.}

\item{metalearner_loss}{Metalearner loss.}

\item{method_settings}{Optional method-specific settings.}

\item{progress}{Logical. Emit per-method progress messages.}
}
\value{
\code{test_data} with \code{.meta_predicted_score} appended.
}
\description{
Runs the inner OOF loop to derive metalearner weights, then fits each base
learner on the full training split and predicts on the test split one at a
time, accumulating a weighted average before freeing each model. This keeps
at most one fitted base learner in memory per worker at any instant, avoiding
the ~11x model accumulation that causes the RAM spike during parallel
crossfit.
}
\keyword{internal}
