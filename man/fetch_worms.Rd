% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{fetch_worms}
\alias{fetch_worms}
\title{Fetch WoRMS metadata for species names}
\usage{
fetch_worms(species, cache_path = NULL, refresh = FALSE)
}
\arguments{
\item{species}{Character vector of scientific species names to query.}

\item{cache_path}{Optional path to an \code{.rds} cache file. When supplied, the
function reads from that cache when it already exists and \code{refresh} is
\code{FALSE}.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache file and
fetch fresh WoRMS results.}
}
\value{
A tibble containing only registry-aligned species trait columns that
are defined in the trait registry. Direct name overlaps from WoRMS are
filled automatically, and \code{genus} / \code{species} are derived from the queried
or returned scientific name when needed.
}
\description{
Queries WoRMS with \code{\link[worrms:wm_records_name]{worrms::wm_records_name()}} and returns the raw WoRMS
metadata in a standardized tibble format. The output is restricted to the
species-level trait names that are present in the package trait registry and
can be supplied directly by WoRMS. The function does not rank, score, or
select among multiple returned records. If WoRMS returns multiple matches for
one queried species, all of them are retained.
}
\examples{
\dontrun{
fetch_worms(c("Clupea pallasii", "Sardinops sagax"))
}

}
