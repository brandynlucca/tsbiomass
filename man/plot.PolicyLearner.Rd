% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy_learner_s7.R
\name{plot.PolicyLearner}
\alias{plot.PolicyLearner}
\title{Plot a \code{PolicyLearner}}
\arguments{
\item{x}{A \link{PolicyLearner} object.}

\item{y}{Unused.}

\item{type}{Figure family to draw.}

\item{view}{Secondary plot selector used for \code{type = "residuals"} and
\code{type = "selected_policy_counts"}.}

\item{outcome}{Outcome variant used for learner calibration diagnostics.
\code{"modeled"} uses the modeled target stored in \code{.outcome} when available.
\code{"raw"} uses \code{.outcome_raw} when available and otherwise falls back to the
configured outcome column.}

\item{rows}{Row subset used for learner diagnostics. \code{"all"} uses all stored
cross-fitted prediction rows. \code{"selected"} uses the score-minimizing rows
retained during post-selection calibration.}

\item{n_bins}{Number of quantile bins used for
\code{type = "calibration_curve"}.}

\item{...}{Unused additional arguments.}
}
\value{
A ggplot object.
}
\description{
Uses the package's S7 method on \code{\link[base:plot]{base::plot()}} so the stored cross-fitted
learner predictions and post-selection calibration summaries can be drawn
directly from the learner object without rebuilding diagnostic tables in user
code.
}
\examples{
\dontrun{
learner <- as_policy_learner(selector)
learner <- crossfit(learner)
learner <- fit(learner)
learner <- calibrate_uncertainty(learner)
plot(learner, type = "predicted_vs_observed")
plot(learner, type = "calibration_curve", outcome = "raw")
plot(learner, type = "residuals", view = "by_policy", outcome = "raw")
}
}
