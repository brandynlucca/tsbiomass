% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow_config_s7.R
\name{as_configurer}
\alias{as_configurer}
\title{Coerce a complete workflow config to \code{Configurer}}
\usage{
as_configurer(
  config,
  base_dir = getwd(),
  registry_path = NULL,
  policy_path = NULL
)
}
\arguments{
\item{config}{A complete nested workflow-config list, a YAML path, or an
existing \code{Configurer} object.}

\item{base_dir}{Base directory used to resolve relative paths.}

\item{registry_path}{Optional trait-registry path used during validation.}

\item{policy_path}{Optional policy-registry path used during validation.}
}
\value{
A validated \code{Configurer} object whose \verb{@data} slot contains the
normalized workflow config list.
}
\description{
Coerce a complete workflow config to \code{Configurer}
}
\examples{
cfg <- as_configurer(list(
  paths = list(
    input = "input.xlsx",
    output_root = "outputs",
    cache_folder = "cache",
    support_folder = "supplemental",
    area_file = "fao_areas.csv",
    log_path = "outputs/workflow.log"
  ),
  workflow = list(
    strict_pdf = FALSE,
    run_multiplier = FALSE,
    write_log = FALSE
  ),
  tuning = list(
    species_model_limit = 2L,
    resamples = 8L
  ),
  similarity = list(
    alpha = 0.8,
    kernel_scale = 4,
    key_metadata_max = 0.25,
    core_weight_cutoff = 0.8,
    conformal_alpha = 0.1,
    species_traits = list(genus = 2, family = 1),
    study_traits = list(frequency = 1, fao_area = 1),
    coherence = list(
      length = list(mode = "overlap", weight = 2, min = 0.25),
      depth = list(mode = "overlap", weight = 3, min = 0.25),
      frequency = list(mode = "overlap", weight = 2, gap = 60)
    )
  ),
  policies = list(
    active = "all_models_weighted"
  ),
  metalearner = list(
    selection_method = "glm"
  )
))
cfg@data$paths$out_root

}
