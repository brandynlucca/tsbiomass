% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alchemist_s7.R
\name{dropout_trait}
\alias{dropout_trait}
\title{Run sigma-dropout sensitivity for one trait column}
\usage{
dropout_trait(
  fc,
  pair_data,
  anchor_idx,
  donor_idx,
  sl_fit,
  donor_sigma_mat,
  target_sigma,
  baseline_sigma_rmse,
  scale
)
}
\arguments{
\item{fc}{Character. Feature column to zero out.}

\item{pair_data}{Tibble of training pairs.}

\item{anchor_idx}{Integer vector of anchor indices from \code{pair_data$.anchor_idx}.}

\item{donor_idx}{Integer vector of donor indices from \code{pair_data$.donor_idx}.}

\item{sl_fit}{A \code{"SuperLearner"} or \code{"Mahalanobis"} learner object.}

\item{donor_sigma_mat}{nxn donor sigma matrix from \code{\link[=build_pair_data]{build_pair_data()}}.}

\item{target_sigma}{Length-n target sigma vector.}

\item{baseline_sigma_rmse}{Scalar baseline sigma RMSE with no feature zeroed.}

\item{scale}{Kernel bandwidth passed to \code{\link[=eval_sigma_rmse]{eval_sigma_rmse()}}.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{trait}, \code{feature_col},
\code{delta_rmse}.
}
\description{
Sets the named feature column to zero in the pair data (equivalent to
removing that trait's contribution to pairwise distance), re-predicts
distances with the trained learner, and evaluates the change in
kernel-weighted sigma RMSE relative to the baseline.  This mirrors the
component-dropout logic in \code{\link[=score_dropout_task]{score_dropout_task()}}, applied to the
Alchemist's learned distance function.
}
\keyword{internal}
