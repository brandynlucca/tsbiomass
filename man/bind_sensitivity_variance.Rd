% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{bind_sensitivity_variance}
\alias{bind_sensitivity_variance}
\title{Decompose sensitivity variance}
\usage{
bind_sensitivity_variance(
  sens_data,
  log_multiplier_col = "log_multiplier_central",
  policy_error_col = "mean_species_median_abs_log"
)
}
\arguments{
\item{sens_data}{Named list returned by \code{\link[=bind_sensitivity_data]{bind_sensitivity_data()}}, with at
least \code{anchor_selected} and \code{select_ref} elements.}

\item{log_multiplier_col}{Name of the log-scale central prediction column in
\code{anchor_selected}. Defaults to \code{"log_multiplier_central"}.}

\item{policy_error_col}{Name of the policy-level mean error column in
\code{select_ref}. Defaults to \code{"mean_species_median_abs_log"}.}
}
\value{
A list with three elements:
\itemize{
\item \code{between_scenario}: per-anchor SD and IQR of log-multiplier across
scenarios.
\item \code{within_scenario_policy}: per-scenario SD and IQR of policy-level mean
error across policies.
\item \code{summary}: one-row summary with global SDs and variance ratio.
}
}
\description{
Computes a two-component variance decomposition from the output of
\code{\link[=bind_sensitivity_data]{bind_sensitivity_data()}}:
}
\details{
\itemize{
\item \strong{Between-scenario} (\code{between_scenario}): per-anchor spread of the
selected log-multiplier across scenarios. Quantifies how much the biomass
correction changes when the analysis pipeline is stressed.
\item \strong{Within-scenario policy} (\code{within_scenario_policy}): per-scenario spread
of \code{mean_species_median_abs_log} across policies. Quantifies how much the
specific policy choice within a fixed scenario matters.
}

A \code{variance_ratio} column in \code{summary} expresses the between-scenario SD as
a fraction of the within-scenario SD, giving a rough relative importance of
"how we set up the analysis" versus "which policy we pick given the setup".
}
