% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{write_scorecard}
\alias{write_scorecard}
\title{Write a Scorecard report}
\usage{
write_scorecard(object, ...)
}
\arguments{
\item{object}{A \link{Scorecard} object.}

\item{...}{Method-specific arguments such as \code{path} and \code{overwrite}.}
}
\value{
The written path, invisibly.
}
\description{
Writes one plain-text record per anchor from a \link{Scorecard}'s selected
policies: the selected policy and its branch, the selection tier, the donor
models actually used, the predicted TS-length equation, the biomass
multiplier and its interval (or why it is not computable, for external
anchors with no baseline TS model), and the total post-selection
uncertainty.
}
\details{
The format is a small, fixed subset of Markdown (\verb{## <anchor>} headings,
\code{- Label: value} fields) - readable as plain text on its own, and simple
enough for \code{\link[=read_scorecard]{read_scorecard()}} to parse back into a tibble. It is not a
full diagnostic export; see the \code{Scorecard} slots directly for the full
per-policy tables.
}
