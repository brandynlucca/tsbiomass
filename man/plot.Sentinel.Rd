% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinel.R
\name{plot.Sentinel}
\alias{plot.Sentinel}
\title{Plot a \code{Sentinel}}
\usage{
\method{plot}{Sentinel}(x, y = NULL, type = "validation", ...)
}
\arguments{
\item{x}{A \link{Sentinel} object.}

\item{y}{Unused.}

\item{type}{Report type: \code{"validation"}, \code{"ablation"},
\code{"ablation_decomposition"}, or \code{"coverage"}.}

\item{...}{Additional arguments forwarded to the \link{Scorecard} plot method
(for example \code{view}, \code{anchor_model_id}, \code{scale}).}
}
\value{
A ggplot object.
}
\description{
Runs \code{summary(x, type = type)} to build the underlying \link{Scorecard} report,
then plots it. Equivalent to calling \code{plot(summary(x, type = type), ...)}
directly, without needing to know the two-step summarize-then-plot path or
the \verb{sentinel_*} type aliases \link{plot.Scorecard} accepts.
}
\examples{
\dontrun{
sentinel <- run_sentinel(object, scenarios)
plot(sentinel, type = "ablation")
}
}
