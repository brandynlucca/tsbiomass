% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{enrich_species_db}
\alias{enrich_species_db}
\title{Enrich species traits from multiple database inputs}
\usage{
enrich_species_db(
  db_list,
  precedence = NULL,
  registry_path = NULL,
  cache_path = NULL,
  missing_tokens = c("-9999")
)
}
\arguments{
\item{db_list}{Named list of species-level data frames or tibbles.}

\item{precedence}{Character vector giving the database precedence order. When
\code{NULL}, the order of \code{db_list} is used.}

\item{registry_path}{Optional path to a trait-registry JSON file. When
\code{NULL}, the installed package registry is used.}

\item{cache_path}{Optional path to an \code{.rds} cache file. When supplied, the
enriched species table is written to that path before returning.}

\item{missing_tokens}{Character vector of placeholder values that should be
treated as missing in addition to \code{NA}.}
}
\value{
A tibble containing one row per species and the registry-aligned
species trait columns merged by database precedence.
}
\description{
Takes a named list of species-level database tables, standardizes missing
placeholders, collapses duplicate rows within each input table, and merges
the species traits across tables by an explicit database precedence order.
}
\examples{
\dontrun{
enrich_species_db(
  list(
    worms = fetch_worms(c("Clupea pallasii")),
    fishbase = fetch_fishbase(c("Clupea pallasii"))
  ),
  precedence = c("worms", "fishbase")
)
}

}
