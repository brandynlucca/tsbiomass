% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alchemist.R
\name{screen_admissibility}
\alias{screen_admissibility}
\title{Screen admissibility across a reference-anchor set}
\usage{
screen_admissibility(
  reference_anchors = NULL,
  candidate_models,
  config = NULL,
  cache_path = NULL,
  refresh = NULL,
  progress = NULL,
  registry_path = NULL,
  keep_training_data = FALSE
)
}
\arguments{
\item{reference_anchors}{Anchor table, typically from
\code{\link[=set_reference_anchors]{set_reference_anchors()}}. When \code{candidate_models} is a \link{Candidates}
object, this may be left \code{NULL} to use its reference anchors.}

\item{candidate_models}{Candidate-model table or a \link{Candidates} object.}

\item{config}{Optional JSON path or list with similarity/anchor settings.}

\item{cache_path}{Optional \code{.rds} cache path.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache.}

\item{progress}{Logical scalar controlling stage messages.}

\item{registry_path}{Optional path to the trait-registry JSON.}

\item{keep_training_data}{Logical scalar. When \code{candidate_models} is an
\link{Alchemist}, the returned object drops \code{pair_data}/\code{trait_mats} (the
distance-learner training-only inputs) by default; pass \code{TRUE} to keep
them, e.g. to still call \code{\link[=distill_traits]{distill_traits()}} afterward.}
}
\value{
When \code{candidate_models} is a \link{Candidates} object, returns that
object containing the admissibility-screen result.
Otherwise, returns a list containing per-anchor results plus bound
score/summary tables.
}
\description{
Screens every reference anchor against the candidate-model pool and returns
the bound score, overlap, gate, and anchor-summary tables.
}
\examples{
\dontrun{
candidates <- build_candidates(list(
  study = list(path = "input.xlsx"),
  anchors = list(selector = list(regional_body = "SWFSC"))
))
candidates <- prepare_similarities(candidates)
candidates <- forge_distances(candidates)
candidates <- screen_admissibility(candidate_models = candidates)
candidates
}

}
