% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinel_s7.R
\name{run_sentinel_smoke_suite}
\alias{run_sentinel_smoke_suite}
\title{Run a reduced smoke Sentinel suite across split specifications}
\usage{
run_sentinel_smoke_suite(
  data,
  workflow_fn,
  config = NULL,
  split_specs = list(anchor_row_holdout = list(split_mode = "anchor_row_holdout"),
    study_holdout = list(split_mode = "study_holdout"), study_cell_holdout =
    list(split_mode = "study_cell_holdout"), species_holdout = list(split_mode =
    "species_holdout")),
  output_dir = NULL,
  extra_species = 0L,
  drop_missing_species = TRUE,
  case_studies = character(0),
  options = NULL,
  refresh = FALSE,
  max_folds = NULL,
  progress = NULL
)
}
\arguments{
\item{data}{Candidate-model table, \link{Candidates}, or \link{PolicySelector}.}

\item{workflow_fn}{User-supplied workflow function passed through to
\code{\link[=run_sentinel_smoke]{run_sentinel_smoke()}}.}

\item{config}{Optional config list or staged object carrying config.}

\item{split_specs}{Named list of split specifications. Each element may
supply \code{deployment_target}, \code{split_mode}, \code{split_col}, \code{scenario_grid},
\code{case_studies}, and \code{options}.}

\item{output_dir}{Optional root output directory for the suite. Each split
run is written beneath its own subdirectory.}

\item{extra_species}{Integer number of additional non-anchor species to
retain in the reduced smoke fixture.}

\item{drop_missing_species}{Logical scalar controlling whether rows with
blank or missing species labels are removed before reduction.}

\item{case_studies}{Optional default holdout IDs for which artifacts should
be persisted.}

\item{options}{Optional default Sentinel options list.}

\item{refresh}{Logical scalar forwarded to \code{\link[=run_sentinel]{run_sentinel()}}.}

\item{max_folds}{Optional integer fold cap forwarded to \code{\link[=run_sentinel]{run_sentinel()}}.}

\item{progress}{Optional logical override forwarded to \code{\link[=run_sentinel]{run_sentinel()}}.}
}
\value{
A named list of \link{Sentinel} objects, one per split specification.
}
\description{
Run a reduced smoke Sentinel suite across split specifications
}
