% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{read_pelagic_db}
\alias{read_pelagic_db}
\title{Fetch pelagic trait-database metadata for species names}
\usage{
read_pelagic_db(species, dl_path, cache_path = NULL, refresh = FALSE)
}
\arguments{
\item{species}{Character vector of scientific species names to query.}

\item{dl_path}{Directory containing the pelagic trait-database files.}

\item{cache_path}{Optional path to an \code{.rds} cache file. When supplied, the
function reads from that cache when it already exists and \code{refresh} is
\code{FALSE}.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache file and
rebuild the result from the available source files.}
}
\value{
A tibble containing only registry-aligned species trait columns that
are defined in the trait registry.
}
\description{
Reads whichever pelagic trait-database CSV files are present in a directory
and returns registry-aligned species metadata. Source-column aliases are
loaded from the external alias template, and the trait registry determines
the final accepted output columns.
}
\examples{
\dontrun{
read_pelagic_db(
  c("Clupea pallasii", "Sardinops sagax"),
  dl_path = "/path/to/pelagic_trait_files"
)
}

}
