% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policylearner.R
\name{screen_learners.PolicyLearner}
\alias{screen_learners.PolicyLearner}
\title{Screen \code{PolicyLearner} base learners}
\arguments{
\item{object}{A \link{PolicyLearner}.}

\item{stage}{Parent learner section to screen: \code{"selection"} or
\code{"uncertainty"}.}

\item{n_folds}{Optional explicit screening-fold override. When omitted, the
method requires \verb{<stage>$screen_learners$n_folds}.}

\item{seed}{Optional screening seed override. When omitted, the method uses
\verb{<stage>$screen_learners$seed}, then the parent learner seed.}

\item{progress}{Optional logical scalar controlling progress messages.}

\item{weight_tolerance}{Numeric tolerance for treating screening weights as
nonzero in the scorecard recommendation flag.}

\item{config}{Optional config override.}
}
\value{
A \link{Scorecard} with learner-screening diagnostics.
}
\description{
Runs a reduced-fold diagnostic screen for the configured \code{selection} or
\code{uncertainty} Super Learner library and returns the results as a regular
\link{Scorecard}. The method is intentionally explicit: it only runs when the
requested parent config section contains \code{screen_learners} or the caller
supplies a screening override such as \code{n_folds}.
}
\details{
The screen inherits the parent learner's method, \code{super_methods},
\code{method_settings}, inner folds, outcome, feature columns, workers, and other
fitting controls. It should not be reported as final validation evidence and
should not be used to tune learner libraries after inspecting final Sentinel
results. Use the returned scorecard with \code{\link[=update_learners]{update_learners()}} to produce a
pruned \link{Configurer}, then run the normal full workflow with that fixed
configuration.
}
