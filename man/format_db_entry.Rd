% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{format_db_entry}
\alias{format_db_entry}
\title{Format one manual database entry against the trait registry}
\usage{
format_db_entry(
  entry,
  scope = c("species", "study", "all"),
  registry_path = NULL,
  drop_unknown = FALSE
)
}
\arguments{
\item{entry}{Named list containing one manual database entry.}

\item{scope}{Character scalar indicating which registry scope to use.
Allowed values are \code{"species"}, \code{"study"}, and \code{"all"}.}

\item{registry_path}{Optional path to a trait-registry JSON file. When
\code{NULL}, the installed package registry is used.}

\item{drop_unknown}{Logical scalar. If \code{TRUE}, input names that are not in
the registry are silently dropped. If \code{FALSE}, the function errors on
unknown names.}
}
\value{
A one-row tibble aligned to the requested registry scope.
}
\description{
Takes a named list, checks that each supplied field exists in the trait
registry, coerces values to the registry-defined type, and returns a
one-row tibble with the full registry-aligned schema for the requested
scope.
}
\examples{
\dontrun{
format_db_entry(list(
  genus = "Clupea",
  species = "pallasii",
  body_shape = "fusiform",
  trophic = 3.8
))
}

}
