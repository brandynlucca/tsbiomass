% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policyselector.R
\name{PolicySelector-class}
\alias{PolicySelector-class}
\alias{PolicySelector}
\title{Policy Selection S7 Classes}
\description{
\code{PolicySelector} is the object-oriented wrapper around the policy
benchmarking, uncertainty calibration, global policy selection, and
anchor-facing policy prediction layers.
}
\details{
It is constructed from a staged \link{Candidates} object and then advanced
through four high-level methods:
\itemize{
\item \code{\link[=benchmark]{benchmark()}} to run the policy benchmark
\item \code{\link[=calibrate_uncertainty]{calibrate_uncertainty()}} to calibrate policy intervals
\item \code{\link[=select_policies]{select_policies()}} to summarize global policy performance
\item \code{\link[=predict]{predict()}} to generate per-anchor policy predictions
}

The final \code{predict()} call returns a \link{PolicyPredictions} object rather than
modifying the selector in place.
}
\examples{
\dontrun{
selector <- as_policyselector(candidates)
selector <- benchmark(selector)
selector <- calibrate_uncertainty(selector)
selector <- select_policies(selector)
predictions <- predict(selector)
predictions@selections
}

}
