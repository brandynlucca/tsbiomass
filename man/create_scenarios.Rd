% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinel.R
\name{create_scenarios}
\alias{create_scenarios}
\title{Create a named Sentinel scenario grid}
\usage{
create_scenarios(
  trait_ablations = NULL,
  gate_ablations = NULL,
  model_ablations = NULL,
  schema_scenarios = NULL,
  baseline_label = "baseline"
)
}
\arguments{
\item{trait_ablations}{Optional character vector for automatic one-at-a-time
ablations, or a named list of character vectors for grouped ablations.
Each element becomes one scenario with \code{exclude_similarity_traits} set to
that vector. The source columns and downstream policy action space remain
unchanged.}

\item{gate_ablations}{Optional named list of character vectors. Each element
becomes one scenario with \code{exclude_admissibility_traits} set to that vector,
relaxing the corresponding hard admissibility gate. Distinct from
\code{trait_ablations}, which prunes similarity/distance features.}

\item{model_ablations}{Optional named list of named lists. Each element
becomes one scenario with \code{drop_rows} set to the supplied candidate-model
column-value filters, so matching models are removed from the fold-local
train/test slices before the workflow runs.}

\item{schema_scenarios}{Optional named list of additional scenario specs.}

\item{baseline_label}{Baseline scenario name.}
}
\value{
Named list.
}
\description{
Create a named Sentinel scenario grid
}
\examples{
scenarios <- create_scenarios(
  trait_ablations = list(
    no_taxonomy = c("family", "genus")
  ),
  model_ablations = list(
    no_fixed_slope = list(equation_form = "fixed_slope"),
    no_policy_a = list(policy = "policy_a")
  )
)
names(scenarios)
scenarios$no_policy_a$drop_rows

}
