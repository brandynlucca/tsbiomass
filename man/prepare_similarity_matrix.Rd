% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{prepare_similarity_matrix}
\alias{prepare_similarity_matrix}
\title{Prepare similarity inputs}
\usage{
prepare_similarity_matrix(
  candidate_models,
  species_traits = NULL,
  study_traits = NULL,
  alpha = NULL,
  k_species = NULL,
  k_study = NULL,
  config = NULL,
  registry_path = NULL,
  seed = NULL
)
}
\arguments{
\item{candidate_models}{Prepared candidate-model table.}

\item{species_traits}{Optional species-trait specification. Use \code{NULL} to
use all eligible species traits at weight \code{1}; a character vector to use
only those traits at weight \code{1}; a named list or named numeric vector to
set explicit starting weights; or a data frame with \code{trait}/\code{weight}
columns. When \code{NULL}, a config-supplied value is used when present.}

\item{study_traits}{Optional study-trait specification. Follows the same
rules as \code{species_traits}. When \code{NULL}, a config-supplied value is used
when present.}

\item{alpha}{Optional starting species-versus-study mixing parameter. When
\code{NULL}, a config-supplied value is used when present.}

\item{k_species}{Optional starting species-distance kernel parameter. When
\code{NULL}, a config-supplied value is used when present.}

\item{k_study}{Optional starting study-distance kernel parameter. When
\code{NULL}, a config-supplied value is used when present.}

\item{config}{Optional JSON path or list with similarity options. Supported
entries are \code{species_traits}, \code{study_traits}, \code{alpha}, \code{k_species},
\code{k_study}, \code{seed}, \code{length_coherence}, \code{depth_coherence},
\code{frequency_coherence}, \code{alpha_grid}, \code{k_species_grid}, and
\code{k_study_grid}.}

\item{registry_path}{Optional path to a trait-registry JSON file.}

\item{seed}{Optional integer seed. When \code{NULL}, a config-supplied value is
used when present; otherwise one is generated and returned in the output
object.}
}
\value{
A list containing the normalized tuning configuration, selected
traits, starting weights, expanded species/study matrices, and collapsed
species profiles.
}
\description{
Selects registry-defined species and study traits, applies starting weights,
expands set-valued traits to binary membership columns, and returns the
prepared species-level and study-level matrices needed for later similarity
calculations.
}
