% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alchemist_s7.R
\name{fit_super_learner}
\alias{fit_super_learner}
\title{Fit a Super Learner ensemble for Alchemist pairwise distance learning}
\usage{
fit_super_learner(
  training_data,
  feature_cols,
  methods,
  outcome_transform = "log1p",
  lambda_rule = "lambda.1se",
  inner_folds = 5L,
  seed = 42L,
  method_settings = NULL,
  workers = 1L,
  progress = FALSE
)
}
\arguments{
\item{training_data}{Tibble produced by \code{\link[=build_pair_data]{build_pair_data()}}.
Must contain \code{.outcome}, optionally \code{.split_group}, and all columns named
by \code{feature_cols}.}

\item{feature_cols}{Character vector of Gower distance column names
(e.g. \code{.dist_swimbladder_type}).}

\item{methods}{Character vector of base learner method names. See
\code{\link[=resolve_learner_methods]{resolve_learner_methods()}} for the supported set.}

\item{outcome_transform}{One of \code{"log1p"} or \code{"identity"}. Applied to
\code{.outcome} before fitting; predictions are back-transformed before
storage.}

\item{lambda_rule}{Lambda selection rule for glmnet-family base learners.
One of \code{"lambda.1se"} or \code{"lambda.min"}.}

\item{inner_folds}{Integer number of cross-validation folds.}

\item{seed}{Integer random seed for fold assignment and base learner fits.}

\item{method_settings}{Named list of per-family tuning overrides (same
structure as \code{metalearner.method_settings} in the config YAML).}

\item{workers}{Integer number of parallel workers. Workers are assigned one
method each. \code{1L} runs sequentially.}

\item{progress}{Logical. Emit \code{\link[=tsb_message]{tsb_message()}} progress lines when \code{TRUE}.}
}
\value{
A list with class \code{"SuperLearner"} containing:
\describe{
\item{\code{fit}}{Named list of final base learner objects
(\code{"BaseLearner"}).}
\item{\code{weights}}{Named numeric vector of NNLS ensemble weights.}
\item{\code{feature_cols}}{Character vector of feature column names.}
\item{\code{outcome_transform}}{The transform applied during training.}
\item{\code{oof_ensemble_prediction}}{Numeric vector of OOF ensemble
predictions in the \strong{original} (back-transformed) scale.}
\item{\code{oof_predictions}}{Tibble of per-method OOF predictions in
original scale.}
\item{\code{oof_performance}}{Tibble with \code{method}, \code{rmse}, and \code{mae}
columns.}
}
}
\description{
Trains a stacked ensemble over a library of base learners using K-fold
out-of-fold (OOF) cross-validation. Base learner fits are parallelised
across methods when \code{workers > 1}. NNLS stacking weights are computed from
the OOF predictions; a final round of base learner fits on the full training
set is then used for prediction at inference time.
}
\details{
This function is entirely independent of the policy-learner path in
\code{meta_policy.R}. It is purpose-built for learning pairwise acoustic
distances from Gower trait features.
}
\keyword{internal}
