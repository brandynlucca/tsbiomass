% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{read_continental_db}
\alias{read_continental_db}
\title{Fetch continental shelf trait-database metadata for species names}
\usage{
read_continental_db(species, db_path, cache_path = NULL, refresh = FALSE)
}
\arguments{
\item{species}{Character vector of scientific species names to query.}

\item{db_path}{Path to the
\code{TraitCollectionFishNAtlanticNEPacificContShelf.xlsx} file.}

\item{cache_path}{Optional path to an \code{.rds} cache file. When supplied, the
function reads from that cache when it already exists and \code{refresh} is
\code{FALSE}.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache file and
rebuild the result from the source file.}
}
\value{
A tibble containing only registry-aligned species trait columns that
are defined in the trait registry.
}
\description{
Reads the \code{TraitCollectionFishNAtlanticNEPacificContShelf.xlsx} workbook and
returns registry-aligned species metadata from the \verb{Trait values} sheet.
Source-column aliases are loaded from the external alias template, and the
trait registry determines the final accepted output columns.
}
\examples{
\dontrun{
read_continental_db(
  c("Clupea pallasii", "Sardinops sagax"),
  db_path = "/path/to/TraitCollectionFishNAtlanticNEPacificContShelf.xlsx"
)
}

}
