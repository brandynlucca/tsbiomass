% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/configure.R
\name{build_configurer}
\alias{build_configurer}
\title{Create a \code{Configurer}}
\usage{
build_configurer(
  config,
  base_dir = getwd(),
  registry_path = NULL,
  policy_path = NULL
)
}
\arguments{
\item{config}{A config list or a YAML path.}

\item{base_dir}{Base directory used to resolve relative paths.}

\item{registry_path}{Optional trait-registry path used during validation.}

\item{policy_path}{Optional policy-registry path used during validation.}
}
\value{
A validated \link{Configurer} object whose \verb{@data} slot contains the
normalized config list.
}
\description{
Create a \code{Configurer}
}
\examples{
cfg <- build_configurer(list(
  paths = list(
    input = "input.xlsx",
    output_root = "outputs",
    cache_folder = "cache",
    support_folder = "supplemental",
    log_path = "outputs/run.log"
  ),
  execution = list(
    strict_pdf = FALSE,
    run_multiplier = FALSE,
    write_log = FALSE
  ),
  tuning = list(
    species_model_limit = 2L,
    resamples = 8L
  ),
  similarity = list(
    alpha = 0.8,
    kernel_scale = 4,
    core_weight_cutoff = 0.8,
    conformal_alpha = 0.1,
    species_traits = list(genus = 2, family = 1),
    study_traits = list(frequency = 1, fao_area = 1),
    coherence = list(
      length = list(mode = "overlap", weight = 2),
      depth = list(mode = "overlap", weight = 3),
      frequency = list(mode = "overlap", weight = 2, gap = 60)
    )
  ),
  admissibility = list(
    key_metadata_max = 0.25,
    coherence = list(
      length = list(mode = "overlap", min = 0.25),
      depth = list(mode = "overlap", min = 0.25),
      frequency = list(mode = "overlap")
    )
  ),
  policies = list(
    active = "closest_within_species"
  ),
  metalearner = list(
    selection_method = "glm"
  )
))
cfg@data$paths$out_root

}
