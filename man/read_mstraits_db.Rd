% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{read_mstraits_db}
\alias{read_mstraits_db}
\title{Read marineSpeciesTraits metadata for species names}
\usage{
read_mstraits_db(species, db_path, cache_path = NULL, refresh = FALSE)
}
\arguments{
\item{species}{Character vector of scientific species names to query.}

\item{db_path}{Path to the \code{marineSpeciesTraits.RData} file.}

\item{cache_path}{Optional path to an \code{.rds} cache file. When supplied, the
function reads from that cache when it already exists and \code{refresh} is
\code{FALSE}.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache file and
rebuild the result from the source file.}
}
\value{
A tibble containing only registry-aligned species trait columns that
are defined in the trait registry.
}
\description{
Reads a \code{marineSpeciesTraits.RData} file, extracts the species-level trait
columns that align with the package trait registry, and returns only the
processed output tibble. The source \code{.RData} object is loaded into a private
environment and removed immediately after the processed table has been built,
so the raw source database is not retained in memory.
}
\examples{
\dontrun{
read_mstraits_db(
  c("Clupea pallasii", "Sardinops sagax"),
  db_path = "/path/to/marineSpeciesTraits.RData"
)
}

}
