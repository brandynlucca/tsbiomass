% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sentinel.R
\name{run_sentinel_fold}
\alias{run_sentinel_fold}
\title{Run one Sentinel fold}
\usage{
run_sentinel_fold(
  object,
  manifest_row,
  workflow_fn = NULL,
  progress = NULL,
  logging = NULL
)
}
\arguments{
\item{object}{A \link{Sentinel} object.}

\item{manifest_row}{One-row Sentinel manifest table, usually from
\code{object@manifest}.}

\item{workflow_fn}{Optional workflow override. Defaults to the workflow stored
on \code{object}.}

\item{progress}{Optional logical override controlling console progress.}

\item{logging}{Optional logical override controlling isolated fold logging.}
}
\value{
A list with \code{ok}, \code{fold_id}, \code{row}, and either \code{fold_result} or
\code{error_message}.
}
\description{
Runs exactly one row from a Sentinel manifest and returns the fold output
without updating the parent Sentinel manifest or result index. This is a
low-level bridge for external orchestrators such as \code{targets}; ordinary
interactive use should call \code{\link[=run_sentinel]{run_sentinel()}} instead.
}
