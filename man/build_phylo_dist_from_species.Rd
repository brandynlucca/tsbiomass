% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{build_phylo_dist_from_species}
\alias{build_phylo_dist_from_species}
\title{Build a phylogenetic distance matrix from a species name vector}
\usage{
build_phylo_dist_from_species(species_vec, genus_vec = NULL)
}
\arguments{
\item{species_vec}{Character vector of species names, one entry per model
row. Binomials ("Gadus morhua") are preferred; single epithets
("morhua") are combined with \code{genus_vec} when provided.}

\item{genus_vec}{Optional character vector of genus names, same length as
\code{species_vec}.}
}
\value{
Numeric n x n matrix with values between 0 and 1, or \code{NULL} on failure.
}
\description{
Uses the Open Tree of Life API (\code{rotl}) to compute cophenetic phylogenetic
distances for a vector of species names, returning a normalised 0 to 1
n x n matrix where \code{n = length(species_vec)}. Rows with missing or
unmatched species names default to distance 1 (maximum dissimilarity).
Returns \code{NULL} when fewer than two species can be matched, so the caller
can apply a rank-based fallback.
}
\keyword{internal}
