% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity.R
\name{construct_gower_distances}
\alias{construct_gower_distances}
\title{Build Gower distance matrices}
\usage{
construct_gower_distances(similarity, progress = NULL)
}
\arguments{
\item{similarity}{Prepared similarity object returned by
\code{\link[=prepare_similarities]{prepare_similarities()}} or a \link{Candidates} object with prepared
similarity state.}

\item{progress}{Logical scalar controlling stage messages.}
}
\value{
When \code{similarity} is a \link{Candidates} object, returns that object with
the Gower-distance bundle attached. Otherwise,
returns a list containing \code{species_dist}, \code{study_dist},
\code{species_dist_model}, \code{combined_dist}, and \code{trait_cols}.
}
\description{
Builds the species-level and study-level Gower distance matrices from a
prepared similarity object, expands the species matrix back to model rows,
and combines the two blocks with the prepared alpha value.
}
\examples{
\dontrun{
candidates <- build_candidates(list(
  study = list(path = "input.xlsx"),
  anchors = list(selector = list(regional_body = "SWFSC"))
))
candidates <- prepare_similarities(candidate_models = candidates)
candidates <- construct_gower_distances(candidates)
candidates
}

}
