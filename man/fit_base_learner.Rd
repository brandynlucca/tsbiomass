% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alchemist_s7.R
\name{fit_base_learner}
\alias{fit_base_learner}
\title{Fit one Alchemist base learner on a training matrix}
\usage{
fit_base_learner(
  x_train,
  y_train,
  method,
  method_settings = NULL,
  seed = NULL,
  lambda_rule = "lambda.1se"
)
}
\arguments{
\item{x_train}{Numeric feature matrix (training rows).}

\item{y_train}{Numeric outcome vector (training rows), in transformed space.}

\item{method}{Character method name. See \code{\link[=resolve_learner_methods]{resolve_learner_methods()}} for
the supported set.}

\item{method_settings}{Named list of per-family tuning settings.}

\item{seed}{Integer random seed.}

\item{lambda_rule}{Character. One of \code{"lambda.1se"} or \code{"lambda.min"}.
Only used by glmnet-family methods.}
}
\value{
A list with class \code{"BaseLearner"} and fields
\code{fit}, \code{method}, \code{family}, \code{lambda_rule}.
}
\description{
Dispatches on \code{family} derived from \code{method}. The outcome vector \code{y_train}
must already be transformed (e.g. \code{log1p}-ed) before calling this function.
}
\keyword{internal}
