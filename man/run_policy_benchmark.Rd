% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy_benchmark.R
\name{run_policy_benchmark}
\alias{run_policy_benchmark}
\title{Run the policy benchmark}
\usage{
run_policy_benchmark(
  candidate_models,
  policy_fun = evaluate_policies,
  curve_fun = predict_policy_curve,
  model_scores = NULL,
  species_lookup = NULL,
  reference_ids = NULL,
  policies = NULL,
  policy_params = list(),
  policy_path = NULL,
  config = NULL,
  include_ts_error = TRUE,
  workers = 1L,
  package_dir = NULL,
  cache_path = NULL,
  refresh = FALSE,
  progress = FALSE,
  registry_path = NULL
)
}
\arguments{
\item{candidate_models}{Prepared candidate-model table.}

\item{policy_fun}{Policy-extraction function. It must accept \code{eval_obj}
plus optional ordination context, and return at least \code{policy} and
\code{multiplier_pred}.}

\item{curve_fun}{Optional policy-curve function for TS-length error
summaries. It must accept \code{policy}, \code{eval_obj}, optional ordination
context, and \code{lengths_cm}.}

\item{model_scores}{Optional ordination score table.}

\item{species_lookup}{Optional species lookup object used by ordination-dependent
policies.}

\item{reference_ids}{Optional vector of reference-model IDs used only to
annotate the output tables.}

\item{config}{Optional JSON path or list with benchmark/admissibility
settings.}

\item{include_ts_error}{Logical scalar. If \code{TRUE}, compute the relative-length
TS error table used by the TS conformal summaries.}

\item{workers}{Number of parallel workers. Use \code{1} for sequential execution.}

\item{package_dir}{Optional package source directory used to load the
development package on parallel workers when running from source.}

\item{cache_path}{Optional \code{.rds} cache path.}

\item{refresh}{Logical scalar. If \code{TRUE}, ignore any existing cache.}

\item{progress}{Logical scalar. If \code{TRUE}, emit lightweight progress updates
during the anchor loop.}

\item{registry_path}{Optional path to the trait-registry JSON.}
}
\value{
A list containing full-pool and leave-one-species-out benchmark
tables.
}
\description{
Evaluates every prepared model as a pseudo-anchor, applies a caller-supplied
policy extractor, and returns in-memory benchmark tables for both the full
donor pool and the leave-one-species-out donor pool.
}
