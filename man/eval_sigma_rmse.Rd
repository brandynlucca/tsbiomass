% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alchemist_s7.R
\name{eval_sigma_rmse}
\alias{eval_sigma_rmse}
\title{Evaluate kernel-weighted sigma RMSE from predicted pairwise distances}
\usage{
eval_sigma_rmse(
  anchor_idx,
  donor_idx,
  dist_vec,
  donor_sigma_mat,
  target_sigma,
  scale
)
}
\arguments{
\item{anchor_idx}{Integer vector of anchor indices (column into sigma matrix).}

\item{donor_idx}{Integer vector of donor indices (row into sigma matrix).}

\item{dist_vec}{Numeric vector of predicted distances, aligned with the index
vectors.}

\item{donor_sigma_mat}{n×n matrix; \verb{[i, j]} = sigma of donor equation i
evaluated at anchor j's length PDF.}

\item{target_sigma}{Length-n numeric vector of each model's own sigma.}

\item{scale}{Positive numeric kernel bandwidth. Distances are fed into
\code{exp(-dist / scale)}; pairs absent from pair_data receive \code{Inf} distance
(weight 0).}
}
\value{
Scalar RMSE, or \code{NA_real_} when no valid anchors remain.
}
\description{
Reconstructs an n×n distance matrix from (donor_idx, anchor_idx, dist_vec)
triplets, then computes the log sigma RMSE of the kernel-weighted ensemble
sigma prediction — the same objective used by \code{\link[=score_similarity_basis]{score_similarity_basis()}} in
the similarity tuning step.
}
\keyword{internal}
